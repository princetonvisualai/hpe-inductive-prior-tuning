"""
Variables to be used across python files
"""

ROOT_DIR = "/usr/username/hpe-inductive-prior-tuning/" # TO DO: Fill in absolute path of hpe-inductive-prior-tuning folder (i.e. /usr/username/hpe-inductive-prior-tuning/). Make sure it ends with /.

PRETRAINED_VGG_PATH = "" # TO DO: Fill in absolute path of downloaded pretrained VGG model

CODE_DIR = ROOT_DIR + "code/"

DATA_DIR = ROOT_DIR + "data/"

TEMPLATES_DIR = ROOT_DIR + "templates/"

CONFIGS_DIR = ROOT_DIR + "configs/"

VOL_DIR = ROOT_DIR + "vol/"

PREPROCESSING_DIR = CODE_DIR + "preprocessing/"

PREPROCESSED_TO_ORIG = PREPROCESSING_DIR + "preprocessed_to_orig.json"

DISTS_DIR = CODE_DIR + "analysis/dists/"

VAR_DIR = CODE_DIR + "analysis/variation/"